# tclaude

A CLI and local browser for durable agentic work across Claude Code, Codex CLI,
OpenCode and Copilot CLI.

Start with the [operating guide](backend-development.md), read the
[common model and architecture](architecture.md), or compare
[harness capabilities](harnesses.md).

The daemon owns agents, conversations, executions, workspaces, correspondence and
attributed work outcomes. Clients use one authenticated application API. State
initialization and offline migration are explicit operations.
